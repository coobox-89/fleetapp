              <%- include('partials/header', { title: "Tutte le Auto", user: user }) %>
<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login | FleetApp</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      background-color: #2C3E50;
      color: #ecf0f1;
    }
    .btn {
      background-color: #3CD071;
      color: white;
      font-weight: bold;
      padding: 0.5rem 1rem;
      border-radius: 9999px;
      transition: all 0.2s ease;
    }
    .btn:hover {
      background-color: #39C574;
      transform: scale(1.05);
      box-shadow: 0 4px 6px rgba(0,0,0,0.3);
    }
  </style>
</head>
<body class="min-h-screen flex flex-col">


  <!-- Main Login Content -->
  <main class="flex-grow flex items-center justify-center px-4">
    <div class="bg-[#34495e] p-8 rounded-lg shadow-lg w-full max-w-sm text-center">
      <h2 class="text-2xl font-bold mb-6 flex items-center justify-center gap-2">
        <i class="fas fa-key text-green-400"></i>
        Login
      </h2>      
      <form method="POST" action="/login" class="space-y-4 text-left">
        <div>
          <label for="email" class="block mb-1">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-2 rounded bg-gray-200 text-black focus:outline-none focus:ring-2 focus:ring-green-400"
          />
        </div>
        <div>
          <label for="password" class="block mb-1">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            required
            class="w-full px-4 py-2 rounded bg-gray-200 text-black focus:outline-none focus:ring-2 focus:ring-green-400"
          />
        </div>
        <button type="submit" class="btn w-full">Login</button>
      </form>
      <p class="mt-4 text-sm">
        Non hai un account?
        <a href="/signup" class="text-green-400 hover:underline">Registrati</a>
      </p>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 text-sm text-center text-white py-2 border-t border-green-400">
    © S2E Sprint SRL 2025 <span class="text-green-400">FleetApp</span>. Tutti i diritti riservati. Made with <span class="text-green-400">💚</span> by <a href="#" class="text-green-400 hover:underline">Mario Cubello</a>
  </footer>

  <script src="https://kit.fontawesome.com/a2e0e6b72f.js" crossorigin="anonymous"></script>
</body>
</html>
